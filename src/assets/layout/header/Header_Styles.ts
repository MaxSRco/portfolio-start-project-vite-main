import styled from "styled-components";

const Header = styled.header`
  background-color: rgba(31, 31, 32, 0.9);
  width: 100%;
  position: fixed;
  padding: 10px 0;
  top: 0;
  right: 0;
  left: 0;
  z-index: 10000;
`

export const S = {
    Header,
}